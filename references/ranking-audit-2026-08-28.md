# WorkBuddy 排序取证记录（2026-08-28）

本页保存一次日期化、只读的维护证据。它不参与日常推荐，也不承诺未来版本继续使用相同服务端协议。

## 取证范围

- WorkBuddy 文件与渲染版本：`5.3.5`。
- 本地专家目录快照：442 个专家、15 个分类；442 条有 `createdAt`，9 条有 `displayPosition`，没有条目提供 `useCount/use_count` 或 `reco_rank`。
- 官方公共目录 `expert_center.json`：匿名响应 `200`，423 个公开专家、15 个分类；`use_count`、`reco_rank`、`published_at` 有效覆盖均为 0。
- 未读取或导出 Cookie、令牌、浏览器存储和其它凭据。

## 官方匿名结果

| 来源 | 匿名结果 | 可用于实时排名 |
|---|---:|---|
| 公共目录 `expert_center.json` | `200`，排名字段覆盖 0 | 否，仅目录快照 |
| 官方实时排名接口 | `401`，`WWW-Authenticate: Bearer` | 否 |
| 官方市场列表的综合、最热、最新排序 | 共用匿名认证门槛 | 否 |

匿名探针固定在 WorkBuddy 第一方 HTTPS 来源，不读取本地凭据，不发送 `Authorization` 或 Cookie，禁用环境代理和重定向。`401/403`、非 JSON、部分字段或只覆盖部分候选都不能升级为可用排名。

## 客户端语义核验

已安装客户端资源表明：综合、最热、最新分别使用 `reco_rank`、`use_count`、`published_at`；热度使用 `useCount`，而 `displayPosition` 只影响运营位置。该结论用于解释字段含义，不把安装包路径、个人目录或机器配置写入运行策略。

需要重新取证时，只使用公开客户端资源、官方文档和无需凭据的官方匿名来源。若结果变化，更新本页日期和证据，再决定是否修改稳定策略。
